#include <unstdio.h>

/**
 * _putchar - prints character to standard output
 * @s: characterr
 * Return: 1 if sucessful and -1 for faiure
 */

int _putchar(char s)
{
	return (write(1, &c, 1));
}
